[project]
name = "blackice-skills"
version = "0.1.0"
description = "Agent skills for BLACKICE"
requires-python = ">=3.11"

dependencies = [
    "blackice-shared",
    "blackice-inference",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
